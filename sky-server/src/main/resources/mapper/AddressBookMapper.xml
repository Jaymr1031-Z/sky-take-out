<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.AddressBookMapper">

    <insert id="save" parameterType="com.sky.entity.AddressBook">
        INSERT INTO address_book (
            user_id, consignee, phone, sex,
            province_code, province_name, city_code, city_name,
            district_code, district_name, detail, label, is_default
        ) VALUES (
                     #{userId}, #{consignee}, #{phone}, #{sex},
                     #{provinceCode}, #{provinceName}, #{cityCode}, #{cityName},
                     #{districtCode}, #{districtName}, #{detail}, #{label}, #{isDefault}
                 )
    </insert>

    <select id="list" resultType="com.sky.entity.AddressBook" parameterType="com.sky.entity.AddressBook">
        select * from address_book
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="isDefault != null">
                and is_default = #{isDefault}
            </if>
            <if test="phone != null">
                and phone = #{phone}
            </if>
        </where>
    </select>

    <update id="update" parameterType="com.sky.entity.AddressBook">
        UPDATE address_book
        <set>
            <if test="consignee != null">, consignee = #{consignee}</if>
            <if test="sex != null">, sex = #{sex}</if>
            <if test="detail != null">, detail = #{detail}</if>
            <if test="label != null">, label = #{label}</if>
            <if test="isDefault != null">, is_default = #{isDefault}</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateIsDefaultByUserId">
        update address_book set is_default = 0 where user_id = #{userId};
    </update>

    <delete id="deleteById" parameterType="com.sky.entity.AddressBook">
        delete from address_book where id = #{id}
    </delete>
</mapper>